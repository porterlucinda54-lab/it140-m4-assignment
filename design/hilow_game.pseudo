# IT 140 Module Four Assignment
# Higher/Lower Game Pseudocode Template
#
# Complete the TODO prompts below with your own pseudocode.
# Keep this file in .pseudo format for submission.
#
# Use clear indentation to show decisions and repeated behavior.
# Your finished pseudocode should address every requirement in the
# current Module Four Assignment Guidelines and Rubric.
design/hilow_game.pseudo
START hilow_game
    # Establish a valid guessing range.
    OUTPUT "Enter the lower bound:"
    INPUT lower_bound
    OUTPUT "Enter the upper bound:"
    INPUT upper_bound

    # Repeat as needed until the bounds satisfy the requirement.
    WHILE lower_bound >= upper_bound
        OUTPUT "Invalid range. The lower bound must be less than the upper bound."
        OUTPUT "Enter the lower bound:"
        INPUT lower_bound
        OUTPUT "Enter the upper bound:"
        INPUT upper_bound
    END WHILE

    # Establish the number the player is trying to guess.
    target_number = RANDOM(lower_bound, upper_bound)

    # Play the game.
    OUTPUT "Guess a number between ", lower_bound, " and ", upper_bound, ":"
    INPUT guess

    # Obtain and validate a guess using the selected bounds.
    WHILE guess < lower_bound OR guess > upper_bound
        OUTPUT "Invalid guess. Enter a number between ", lower_bound, " and ", upper_bound, ":"
        INPUT guess
    END WHILE

    # Repeat the required game behavior until the correct guess.
    WHILE guess != target_number
        # Compare a valid guess and provide the required feedback.
        IF guess < target_number
            OUTPUT "Too low!"
        ELSE
            OUTPUT "Too high!"
        END IF

        # Obtain and validate another guess when play continues.
        OUTPUT "Guess again:"
        INPUT guess

        WHILE guess < lower_bound OR guess > upper_bound
            OUTPUT "Invalid guess. Enter a number between ", lower_bound, " and ", upper_bound, ":"
            INPUT guess
        END WHILE
    END WHILE

    OUTPUT "Correct!"

END hilow_game

# === References ===
# No external references used.
